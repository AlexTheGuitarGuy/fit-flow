# This file is generated by Nx.
# Build the docker image with `nx docker:build @fit-flow/api-gateway`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @fit-flow/api-gateway -p 3000:3000`.
#
FROM docker.io/node:24-alpine3.23 AS build

ENV HOST=0.0.0.0
ENV PORT=3000

RUN corepack enable

WORKDIR /app

COPY ./apps/api-gateway/dist .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

RUN pnpm install --frozen-lockfile

CMD [ "node", "main.js" ]
