{
  "targets": {
    "docker:build": {
      "executor": "nx:run-commands",
      "dependsOn":[ "prune"],
      "options": {
        "command": "docker build -f apps/api-gateway/Dockerfile .",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
